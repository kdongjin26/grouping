# grouping — 25명 5개 조 뽑기 페이지

25명 학생을 5개 조로 분배하는 정적 HTML 페이지. 단일 파일(`index.html`)로 동작.

## 파일 구성

- `index.html` — 페이지 본체 (HTML/CSS/JS 인라인). 학생 데이터도 JS 상단에 하드코딩되어 있음.
- `출석부.pdf` — **현재 기준 원본 명단** (2026-2학기 학부 출석부, 25명). gitignore됨 — 학번 9자리가 들어 있어 public 저장소에 올리면 안 됨.
- `번호표.xlsx` — 구 명단 23명 (gitignore됨). 학과 정보가 출석부와 어긋나므로 참고용으로만.
- `.gitignore` — `.claude/`, `번호표.xlsx`, `_xlsx_extracted/`, `출석부.pdf` 제외

## 학생 데이터 (총 25명)

`출석부.pdf`에서 추출해서 `index.html`의 `STUDENTS` 배열에 직접 넣어둠. 각 학생: `{ n, name, g('남'/'여'), y(22/23/24), dept('일사'/'교육'/'한문'/'윤리') }`. 이름 가나다순, `n`은 1~25.

분포:
- 성별: 남 11, 여 14
- 학번: 22학번 8, 23학번 5, 24학번 12
- 학과: 일사 20, 교육 2, 한문 2, 윤리 1

### 출석부 PDF 파싱 시 주의

pypdf의 단순 `extract_text()`는 4열 그리드를 행 단위로 뭉쳐 읽어 학과-이름 짝을 어긋나게 만들 수 있다. pdfplumber `extract_words()`로 좌표(`x0`)를 받아 열을 나눠라 — 열 경계는 ID `x0` 기준 약 186 / 377 / 569 / 760, 이름 `x0`는 약 56 / 247 / 439 / 630. 학과·학번 줄이 이름 줄보다 위(`top` 약 -74)에 온다.

### 기존 xlsx와의 불일치

`번호표.xlsx`는 김주은=한문 / 김주혜=일사로 적혀 있으나, 출석부 PDF는 그 반대(김주은 202410889 일반사회교육과, 김주혜 202411451 한문교육과)다. 좌표 기반으로 재확인한 결과 **출석부가 맞다**고 보고 그쪽을 채택했다. xlsx는 수기 작성본으로 추정.

### 신규 추가 2명

`김민서`(여, 23학번, 일사), `최인선`(여, 23학번, 한문). 출석부에 성별 칸이 없어 **사용자 확인을 거쳐** 둘 다 여로 입력했다. 명단이 또 바뀌면 신규 인원 성별은 반드시 사용자에게 물어라.

## 조 구성 규칙

- 5개 조 × 5명 (총 25명) — `GROUP_SIZES = [5, 5, 5, 5, 5]`
- 내부 분배 조건 (UI에 노출 안 함):
  - 성별 mix: 모든 조에 남·여 각각 1명 이상
  - 학번 mix: 24학번만으로 구성된 조 없음 (= 22 또는 23학번이 모든 조에 1명 이상)
  - 학과 mix: 일사만 있는 조 없음

### 학과 조건이 이제 완전 충족되는 이유

비-일사가 5명(김재희·김주혜·박효진·이재혁·최인선)이고 조도 5개라 정확히 1명씩 배분된다. 23명/6개 조 시절의 best-effort 제약은 해소됐다. **조 개수를 6개 이상으로 늘리면 다시 best-effort로 돌아간다** — 그때는 사용자에게 안내할 것.

## 분배 알고리즘 (`distribute()`)

`NUM_GROUPS = GROUP_SIZES.length`로 일반화되어 있어 조 개수에 하드코딩 의존이 없다.

1. 비-일사를 셔플된 조에 1명씩 시드 배정 (`seed = min(비-일사 수, NUM_GROUPS)`)
2. 시드에서 남은 비-일사는 일반 풀로 합류 (비-일사가 조 수보다 많은 경우 대비)
3. 나머지를 그리디 분배 (자리 남은 조 중 무작위 선택)
4. 성별·학번 조건 검증, 실패 시 재시도 (최대 4000회)
5. 그래도 실패하면 단순 랜덤 분배로 fallback

검증 실측: Node로 5000회 시행해 조 크기·성별·학번·학과·중복 위반 0건.

## UI 톤

차분한 더스티 파스텔. 첫 안이 너무 발랄(초등학교 느낌)했다는 피드백을 받아 톤 다운한 상태.
- 폰트: Gowun Batang(세리프, 제목/조 번호) + Gowun Dodum(본문)
- 조 색상은 CSS 변수 `--g{1..6}-bg/-ink/-bd`로 관리 (`.group-6`은 현재 미사용이나 조 확장 대비해 남겨둠)
- 두구두구 효과: 명단 카드들의 이름이 약 2.2초간 빠르게 무작위로 바뀌다가 멈춤
- 학생 카드는 번호+이름만 노출 (성별/학번/학과는 노출 안 함, 사용자 요구)

다시 발랄하게 키우거나 더 다듬는 요청이 오면 CSS 변수와 폰트만 바꿔도 큰 폭으로 톤 조절 가능.

## 배포

- GitHub: `https://github.com/kdongjin26/grouping` (public)
- GitHub Pages 설정은 사용자가 직접 (Settings → Pages → main / root)
- 배포 URL (예상): `https://kdongjin26.github.io/grouping/`
- `gh` CLI 미설치 환경 — Pages 자동 enable 불가, 수동 설정 필요
- **배포 확인됨**: `curl`로 200 응답 + 라이브 HTML이 로컬 `index.html`과 바이트 동일함을 확인함

## 작업 시 주의

- 학생 데이터를 수정해야 하면 `index.html`의 `STUDENTS` 배열을 직접 수정 (원본 재파싱 불필요)
- 조 인원/개수 변경은 `GROUP_SIZES` 배열만 바꾸면 됨 (`NUM_GROUPS`가 자동으로 따라감)
- **로컬 렌더 확인은 `file://`로 안 된다** — Chrome 확장이 차단한다. `python -m http.server`로 띄우고 `http://127.0.0.1:<port>/`로 열 것. 확장 충돌(`Cannot access a chrome-extension:// URL of different extension`)이 잦으니, 알고리즘 검증은 Node로 `<script>` 블록에서 `STUDENTS`/`distribute`만 뽑아 돌리는 쪽이 확실하다
- 분배 조건 추가/완화는 `distribute()` 내 검증 블록에서
- 배포 후 변경은 commit → push만 하면 GitHub Pages가 자동 재배포
